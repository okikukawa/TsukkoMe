<h2>ツッコミを登録する</h2>

<div>
  <%= link_to @situation.user.name, user_path(@situation.user) %>
  <%= @situation.created_at.strftime("%Y年%-m月%-d日") %>
  <div >
    <%= @situation.title %>
  </div>
</div>

<%= render 'form' %>
<%= link_to "<< シチュエーション詳細", situation_path(@situation.id) %>
